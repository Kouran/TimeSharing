<p class="titolo">Chiudi questo annuncio</p>
<p id="notice" color="red"><%= notice %></p>
<form action="crea" method="post">
	<%= hidden_field_tag "applicant", Session.current_user%>
	<%= hidden_field_tag "ad_id", params[:id] %>
	<%= text_field_tag "fullfillant" %>
	<%= number_field_tag "amount", nil, min: 0 %>
	<%= submit_tag "Chiudi" %>
</form>
